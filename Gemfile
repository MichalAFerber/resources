source "https://rubygems.org"

# Mirrors the GitHub Pages build toolchain (Jekyll + jekyll-theme-cayman +
# supported plugins). Run `bundle install`, then `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins
